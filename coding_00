#include<iostream>
#include<cstdio>
#include<cstring>
#include<algorithm>
using namespace std;
const int N=30;
int n;
int arr[N];
void dfs(int u)
{
    if(u>n)
    {
        for(int i=1;i<=n;i++)
            if(arr[i]==1)
            printf("%d ",i);
        puts("");
        return;
    }
    arr[u]=0;
    dfs(u+1);
    arr[u]=1;
    dfs(u+1);
}
int main()
{
    cin>>n;
    dfs(1);
    return 0;
}
